import Repo from './__TEMPLATE__FILE__NAME__';

export const repoMock: Repo = {
	create: jest.fn(),
	update: jest.fn(),
	findById: jest.fn(),
	deleteById: jest.fn(),
	findMany: jest.fn(),
}

export default repoMock;
